// === Barrel Export for all Atoms ===
// Importing from here instead of individual files keeps code clean.
// Example: import { BTGButton, BTGInput } from '../atoms';

export { default as BTGButton } from './BTGButton';
export { default as BTGInput }  from './BTGInput';
export { default as BTGBadge }  from './BTGBadge';
